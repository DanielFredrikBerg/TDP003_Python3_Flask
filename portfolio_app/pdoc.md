---
description: |
    API documentation for modules: portfolio_app, portfolio_app.routes, jen_api.

lang: en

classoption: oneside
geometry: margin=1in
papersize: a4

linkcolor: blue
links-as-notes: true
...


    
# Module `portfolio_app` {#portfolio_app}




    
## Sub-modules

* [portfolio_app.routes](#portfolio_app.routes)






    
# Module `portfolio_app.routes` {#portfolio_app.routes}






    
## Functions


    
### Function `home` {#portfolio_app.routes.home}




>     def home()


Homepage. Contains info on author.

**Arguments**:

* None

**Returns**:

* If search has been used, redirect to <code>[list()](#portfolio\_app.routes.list "portfolio\_app.routes.list")</code>. Otherwize:

* Render home page.

    
### Function `list` {#portfolio_app.routes.list}




>     def list()


List of projects, with advanced search.

**Arguments**:

* None

**Returns**:

* Render list page.

    
### Function `not_found_error` {#portfolio_app.routes.not_found_error}




>     def not_found_error(
>         error
>     )


404 Page. Shows if page no found.

**Arguments**:

* <code>error</code> (int) - Error code.

**Returns**:

*  If search has been used, redirect to <code>[list()](#portfolio\_app.routes.list "portfolio\_app.routes.list")</code>. Otherwize:

*  Render 404 page. And:

* <code>404</code> (int), Error code 404.

    
### Function `show_project` {#portfolio_app.routes.show_project}




>     def show_project(
>         id
>     )


Projectpage. Shows info on project with id <code>id</code>.

**Arguments**:

* <code>id</code> (int) - Id of project to show.

**Returns**:

* If search has been used, redirect to <code>[list()](#portfolio\_app.routes.list "portfolio\_app.routes.list")</code>. Otherwize:

* Render 404 page.

    
### Function `techniques` {#portfolio_app.routes.techniques}




>     def techniques()


Techniques page. Shows projects pased on techniques used

**Arguments**:

* None

**Returns**:

* If search has been used, redirect to <code>[list()](#portfolio\_app.routes.list "portfolio\_app.routes.list")</code>. Otherwize:

* Render techniques page.




    
# Module `jen_api` {#jen_api}






    
## Functions


    
### Function `add_project` {#jen_api.add_project}




>     def add_project(
>         db,
>         project
>     )


Adds project to database

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.
* <code>project</code> (dictionary) - Project to be added.

**Returns**:

* None

    
### Function `get_project` {#jen_api.get_project}




>     def get_project(
>         db,
>         id_
>     )


Returns <code>project</code> matching <code>id</code> in <code>db</code>.

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.
* <code>id\_</code> - Projects id.

**Returns**:

* (dictionary) - Project containing project_id: <code>id\_</code>.

    
### Function `get_project_count` {#jen_api.get_project_count}




>     def get_project_count(
>         db
>     )


Returns number of projects in <code>db</code>.

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.

**Returns**:

* (int) - Length of database.

    
### Function `get_search_fields` {#jen_api.get_search_fields}




>     def get_search_fields(
>         db
>     )


Returns available fields to search in <code>db</code>.

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.

**Returns**:

* (list) - List of all keys of dictionaries in <code>db</code>.

    
### Function `get_technique_stats` {#jen_api.get_technique_stats}




>     def get_technique_stats(
>         db
>     )


Returns a dictionary where all techniques used are the keys, and the contents are lists of dictionaries of projects IDs and names.

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.

**Returns**:

* (dictionary) - Dictionary containing lists for every technique, wich in turn contains dictionaries of id and name for every project using that technique.

    
### Function `get_techniques` {#jen_api.get_techniques}




>     def get_techniques(
>         db
>     )


Returns available techniques in <code>db</code>.

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.

**Returns**:

* (list) - List of all techniques used in projects in <code>db</code>.

    
### Function `load` {#jen_api.load}




>     def load(
>         path
>     )


Loads json and returns list sorted on projects ID

**Parameters**:

* <code>path</code> (string) - Path to the json file.

**Returns**:

* (list) - List containing dictionaries of all projects in json file.

    
### Function `main` {#jen_api.main}




>     def main()


Only used for testing purposes.

**Parameters**:

* None

**Returns**:

* None

    
### Function `remove_project` {#jen_api.remove_project}




>     def remove_project(
>         db,
>         id_
>     )


Removes project from 'db' if id matches project.

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.
* <code>id\_</code> - Projects id.

**Returns**:

* None

    
### Function `search` {#jen_api.search}




>     def search(
>         db,
>         sort_by='start_date',
>         sort_order='desc',
>         techniques=None,
>         search=None,
>         search_fields=None
>     )


Will return a list of projects in <code>db</code> matching all of the following search requirements:

1. Only match projects containing all <code>techniques</code>.

2. Only match projects containing string <code>[search()](#jen\_api.search "jen\_api.search")</code> in atleast one field in <code>search\_fields</code>.
        If <code>search\_fields</code> is empty, all fields will be checked.

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.
* <code>sort\_by</code> (string) - Field of projects to sort by.
* <code>sort\_order</code> (string) - Order to sort in. 'asc' for ascending order, 'desc' for descending.
* <code>techniques</code> (list) - Techniques that must be in project to be returned. Field is ignored if empty or None.
* <code>[search()](#jen\_api.search "jen\_api.search")</code> (string) - Free text search.
* <code>search\_fields</code> (list) - Fields to be searched. If empty, no fields are searched. If None, all fields are searched.

**Returns**:

* (list) - A list containing dictionaries for all projects matching given search criteria.

    
### Function `write_db_to_json` {#jen_api.write_db_to_json}




>     def write_db_to_json(
>         db,
>         save_file
>     )


Writes <code>db</code> to file with path <code>save\_file</code>, overwriting existing data.

**Parameters**:

* <code>db</code> (list) - Database, as generated by <code>[load()](#jen\_api.load "jen\_api.load")</code>.
* <code>save\_file</code> - Path to file to write to.

**Returns**:

* None



-----
Generated by *pdoc* 0.9.1 (<https://pdoc3.github.io>).
